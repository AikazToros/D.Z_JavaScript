//Проверить, может ли пользователь сесть за руль, исходя из трех параметров//

const hasLicence = true
const age = 32
isDrunk = false 

console. log(age >= 18 && hasLicence && !isDrunk ? 'Может' : 'Не может' )